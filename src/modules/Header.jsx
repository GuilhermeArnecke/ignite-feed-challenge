import styles from "./Header.module.css"

import todoLogo from "../assets/LogoToDoList.svg"

export function Header() {
    <div className={styles.background}>
      <img className={styles.img} src={todoLogo} alt="Ícone de foguete com ToDo escrito ao lado"/>
    </div>
}